import React from 'react';
import { Layer } from 'react-konva';

export const OverlayLayer: React.FC = () => {
  // This layer can be used for temporary overlays, guides, or other UI elements
  // For now, it's empty but ready for future enhancements
  return <Layer />;
};
